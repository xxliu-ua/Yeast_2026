The deep_fea_extractor folder contains the necessary code for extracting features from the pretrained DenseNet-201
The readme file in the folder contains additional instructions

The code folder contains the SVM implementation in MATLAB
The linear and RBF kernel SVM are both available and ready to use
Make sure that the correct classifier and optimizer are used in the main code
